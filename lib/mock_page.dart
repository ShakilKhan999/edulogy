import 'package:flutter/cupertino.dart';

class MockPage extends StatelessWidget {
  const MockPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("MockPage"),);
  }
}
